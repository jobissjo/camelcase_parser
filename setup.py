from setuptools import setup, find_packages

setup(
    name='camel_case_parser',
    version='0.1',
    packages=find_packages(),
    install_requires=[

    ],
    tests = 'tests',
    requires=[],
    package_dir=[]

)